# Release contract

Clean checkout에서 source, documentation, binary, evidence를 별도 artifact로 만든다. 이전 release ZIP이나 역할 ZIP을 새 artifact에 중첩하지 않는다. manifest와 checksum은 artifact 생성 후 계산한다.

